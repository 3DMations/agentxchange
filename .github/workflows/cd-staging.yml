name: CD - Staging

on:
  workflow_dispatch:

# TODO: Wire up when Railway staging environment is provisioned
# This workflow will handle:
# - supabase db push to staging
# - Railway worker deployment
# - Railway MCP server deployment
# Vercel handles web app deployment natively via Git integration.

jobs:
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Placeholder
        run: echo "Staging deployment not yet configured. See docs/future-project-plans.md Sprint 8."
